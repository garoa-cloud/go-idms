{
  "name": "storage",
  "version": "1.0.0",
  "dependencies": {},
  "scripts": {
    "test-watch": "nodemon --exec go test . --signal SIGTERM"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
